{
  "appType": "http-wasm",
  "description": "Returns USERNAME from env var and PASSWORD from secret in response body",
  "request": {
    "method": "GET",
    "path": "/"
  },
  "dotenv": {
    "enabled": true,
    "path": "."
  }
}
