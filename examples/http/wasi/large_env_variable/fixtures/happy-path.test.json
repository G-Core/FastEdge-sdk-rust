{
  "appType": "http-wasm",
  "description": "LARGE_CONFIG loaded via dictionary API — reports byte count",
  "request": {
    "method": "GET",
    "path": "/"
  },
  "dotenv": {
    "enabled": true,
    "path": "."
  }
}
