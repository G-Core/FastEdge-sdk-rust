{
  "appType": "http-wasm",
  "description": "Country code FR (unmapped) - falls back to BASE_ORIGIN",
  "request": {
    "method": "GET",
    "path": "/test"
  },
  "dotenv": {
    "enabled": true,
    "path": "."
  }
}
