{
  "appType": "http-wasm",
  "description": "No client IP header — returns 500 before opening KV store",
  "request": {
    "method": "GET",
    "path": "/"
  },
  "dotenv": {
    "enabled": true,
    "path": "."
  }
}
