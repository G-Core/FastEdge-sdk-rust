{
  "appType": "http-wasm",
  "description": "Wrong Authorization header value — expects 403 with empty body",
  "request": {
    "method": "GET",
    "path": "/",
    "headers": {
      "authorization": "wrong-password"
    }
  },
  "dotenv": {
    "enabled": true,
    "path": "."
  }
}
