{
  "appType": "http-wasm",
  "description": "Missing PASSWORD env var — expects 500 Misconfigured app",
  "request": {
    "method": "GET",
    "path": "/",
    "headers": {
      "authorization": "any-password"
    }
  }
}
