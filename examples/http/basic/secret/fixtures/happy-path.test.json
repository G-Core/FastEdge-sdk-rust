{
  "appType": "http-wasm",
  "description": "Happy path: secret exists — expects both get() and get_effective_at() to return the secret value",
  "request": {
    "method": "GET",
    "path": "/"
  },
  "dotenv": {
    "enabled": true,
    "path": "."
  }
}
