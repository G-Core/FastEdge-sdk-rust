{
  "appType": "http-wasm",
  "description": "GET with wrong Authorization value → 403 Forbidden (empty body)",
  "request": {
    "method": "GET",
    "path": "/",
    "headers": {
      "authorization": "wrong-password"
    }
  },
  "dotenv": {
    "enabled": true,
    "path": "."
  }
}
