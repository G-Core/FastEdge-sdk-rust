{
  "appType": "http-wasm",
  "description": "GET with no env vars set → 500 Misconfigured app (PASSWORD missing)",
  "request": {
    "method": "GET",
    "path": "/",
    "headers": {
      "authorization": "any-value"
    }
  }
}
